/// 示例表名
pub const TABLE_NAME: &str = "demo_item";

/// 本模块写在 db_meta 里的版本键（与探针键 `ok` 分开）
pub const SCHEMA_VERSION_KEY: &str = "demo.schema_version";

/// 当前 schema 版本
pub const SCHEMA_VERSION: i64 = 1;
